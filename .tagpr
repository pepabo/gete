[tagpr]
	vPrefix = true
	releaseBranch = main
	changelog = true
	# The version lives in the git tag only. Writing it into pyproject.toml
	# would make tagpr rewrite a file that the build derives from the tag
	# anyway, and uv.lock would have to follow every bump.
	versionFile = -
