# Keep tagpr's own release pull requests out of the generated release notes.
# Otherwise every release lists a "Release for vX.Y.Z" entry about itself.
changelog:
  exclude:
    labels:
      - tagpr
