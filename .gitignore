# Byte-compiled / optimized files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
dist/
wheels/
sdist/
*.egg-info/
*.egg
.eggs/
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.tox/
.nox/

# Coverage
.coverage
.coverage.*
htmlcov/
coverage.xml

# Version file that hatch-vcs can write. The version is derived from git
# tags at build time and must never be committed.
src/gete/_version.py

# Terraform (this repository ships a module; state, providers, and provider
# locks belong to the root configurations that call it)
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Environment files and local tooling
.env
.env.*
!.env.example
.direnv/
.claude/settings.local.json
CLAUDE.local.md

# Editors and OS
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
