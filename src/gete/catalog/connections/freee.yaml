id: freee
display_name: freee
docs: https://developer.freee.co.jp/

# The root is the same for every installation - the tenant is chosen by the
# token, not the URL - so the catalog names it. Its host is the only one a
# user token may be sent to, and openapi toolsets build their URLs from it.
base_url: https://api.freee.co.jp
# freee tokens carry no prefix; they are accepted by elimination.
token_prefixes: []

oauth:
  authorization_url: https://accounts.secure.freee.co.jp/public_api/authorize
  token_url: https://accounts.secure.freee.co.jp/public_api/token
  # Permissions are configured on the freee app. The authorization URL carries no scopes.
  scopes: {}
  # Used verbatim as the query string. Gemini Enterprise appends client_id and redirect_uri.
  authorization_query:
    response_type: code

verified:
  gemini_enterprise: 2026-08-20

examples:
  accepts:
    - "a1b2c3d4e5f60718293a4b5c6d7e8f90"
  rejects:
    - "ya29.a0AfH6SMB" # Google access token
    # Claims: {"iss": "https://accounts.google.com"}, as in an ID token.
    - "eyJhbGciOiJSUzI1NiJ9.eyJpc3MiOiJodHRwczovL2FjY291bnRzLmdvb2dsZS5jb20ifQ.sig"
    - "gho_16C7e42F292c6912E7710c838347Ae178B4a" # GitHub
